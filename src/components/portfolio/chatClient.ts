/**
 * @deprecated Import chat APIs from `@/features/chat` instead.
 * Kept as a compatibility export while the legacy widget is migrated.
 */
export {
  ChatApiError,
  sendChatMessage,
} from "../../features/chat/services/chatApi";
export type { ChatMessage } from "../../features/chat/types";
